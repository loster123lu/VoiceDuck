# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/)。

## [1.1.0] - 2026-08-23

### Added

- 新增 WASAPI 实时播放端点监听与混音引擎，可把物理麦克风和当前播放声同时发送给微信、QQ 等通话软件。
- 新增约 350 ms 延迟队列与通话活动回声保护门；对方讲话时丢弃播放分支，麦克风保持在线。
- 内置未修改的标准 VB-CABLE Driver Pack 45，并提供明确的安装、探测与卸载入口。
- 安装前验证驱动压缩包和安装程序 SHA-256、Windows Authenticode 与发布者身份。
- 检测到通话音频会话时显示默认选“否”的风险确认；驱动操作不修改系统默认设备、不自动重启。
- 驱动操作结束后等待 Windows Audio 与真实端点稳定恢复，并提供“恢复声音”按钮处理服务未自动恢复的情况。
- 提供独立麦克风/播放声增益、电平和最终限幅。
- 暂停播放声时保持麦克风在线。
- 分享开始时可自动将 Windows 默认麦克风切换到 CABLE Output，停止、异常结束或退出时恢复原设备。
- 默认麦克风切换前写入崩溃恢复记录；下次启动会恢复被中断的路由。
- 新增 VoiceDuck 专属程序、窗口与托盘图标。
- 新增无需虚拟输出驱动的耳机音量保护：限制默认输出设备的系统音量上限，并按端点输入峰值快速衰减、缓慢恢复。
- 音量保护支持最大系统音量、突发峰值上限与恢复速度设置，默认关闭并由用户明确启用。
- 新增本地人声优先混音；检测到真实麦克风讲话时快速压低分享的播放声，结束后平滑恢复。

### Changed

- 主界面和托盘菜单新增“通话音乐分享”入口。
- 统一设备列表与自动闪避的 Core Audio 类型，修复音频线程运行时打开分享页可能出现的 COM 强制转换失败。
- 分享运行时将“停止分享”显示为高饱和红色，并增加悬停、按下与描边反馈。
- 分享页保留“自动切换并恢复默认麦克风”，取消额外的路由确认勾选，选择设备后可直接开始分享。
- “开始分享”和“停止分享”合并为同一个主按钮；运行时按钮切换为醒目的红色停止状态。
- 修复滑轨点击位置与滑块落点不一致；主界面、音乐分享和音量保护现在均可点击轨道精确定位。
- 音频冒烟测试增加 VB-CABLE 双端点只读探测。
- 修复部分麦克风或通话软件只使用单侧声道时，播放声正常但对方听不到本地讲话的问题；麦克风现统一输出到左右声道。
- 开始分享前等待真实麦克风返回首个音频帧，采集失败时不再切换系统默认输入。
- 构建测试增加实时延迟、回声门消费行为、混音提供器、暂停、限幅以及嵌入驱动包 Authenticode 验证。

## [1.0.0] - 2026-08-22

### Added

- 多输出设备、多音频会话枚举。
- 可分别配置通话触发应用与背景目标应用。
- 防提示音触发延迟、断句保持、Attack 和 Release 调节。
- 按会话原音量比例闪避，并在暂停或退出时恢复。
- 闪避期间的用户手动音量调整检测。
- 中文 WinForms 界面、四组预设、系统托盘和开机启动。
- 核心逻辑测试、音频会话冒烟测试和界面渲染测试。
